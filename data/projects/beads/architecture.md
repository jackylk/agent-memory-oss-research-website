# Beads 架构分析

> 基于实际代码库分析 (Beads v0.49.6+)

## 1. 整体架构概述

**Beads** (命令: `bd`) 是一个**分布式、Git 支持的图表问题追踪器**,专为 AI 编码智能体优化。它是一个**持久化内存系统**,替代混乱的 markdown 计划,使用依赖感知的图表让智能体在长期任务中不失去上下文。

### 项目定位

- **应用领域**:AI 编码智能体任务管理、分布式团队协作、长期项目规划
- **核心价值**:分布式架构 + 版本控制集成 + AI 优化 + 零冲突设计
- **关键创新**:基于哈希的 ID 防止合并冲突、Dolt 版本控制数据库、内存压缩系统

### 核心价值主张

- **分布式架构**:无需中央服务器,通过 Git 分享问题
- **版本控制集成**:Dolt 后端支持细粒度版本历史和分支
- **AI优化**:JSON 输出、依赖追踪、自动任务检测
- **零冲突设计**:基于哈希的 ID 防止多智能体/多分支工作流中的合并冲突
- **内存压缩**:语义记忆衰减总结旧的已关闭任务
- **消息传递**:线程化消息、临时生命周期和邮件委托

### 与其他项目的差异

| 维度 | Beads | Mem0 | Graphiti | Letta |
|------|-------|------|----------|-------|
| **核心概念** | 分布式任务图 | 向量记忆 | 时态知识图谱 | 虚拟上下文 |
| **存储架构** | Dolt + JSONL (分布式) | 向量数据库 + SQL | 图数据库 + 向量 | PostgreSQL + 向量 |
| **主要用途** | 任务追踪和依赖管理 | 通用智能体记忆 | 知识图谱和推理 | 长上下文对话 |
| **Git 集成** | 原生支持 (Git 分支) | 否 | 否 | 有限 |
| **部署方式** | CLI 工具 (本地优先) | API 服务 | API 服务 | API 服务 |
| **版本控制** | 内置 (Dolt 历史) | 依赖外部服务 | 依赖外部服务 | 依赖外部服务 |
| **并发模式** | 多智能体/多分支安全 | 集中式 | 集中式 | 集中式 |

---

## 2. 核心架构组件

### 架构分层图

```architecture
{
  "layers": [
    {
      "title": "CLI 命令层",
      "icon": "⌨️",
      "color": {
        "bg": "bg-blue-100",
        "border": "border-blue-600",
        "textColor": "#1e40af"
      },
      "nodes": [
        { "id": "CLI1", "label": "create, list, update, close" },
        { "id": "CLI2", "label": "ready, show, dep, sync" },
        { "id": "CLI3", "label": "audit, compact, export" },
        { "id": "CLI4", "label": "Cobra 框架" }
      ]
    },
    {
      "title": "存储层",
      "icon": "💾",
      "color": {
        "bg": "bg-blue-100",
        "border": "border-blue-600",
        "textColor": "#1e40af"
      },
      "nodes": [
        { "id": "STG1", "label": "Dolt 后端 (主要)" },
        { "id": "STG2", "label": "SQLite 后端 (遗留)" },
        { "id": "STG3", "label": "内存后端 (测试)" },
        { "id": "STG4", "label": "存储工厂" }
      ]
    },
    {
      "title": "同步层",
      "icon": "🔄",
      "color": {
        "bg": "bg-blue-100",
        "border": "border-blue-600",
        "textColor": "#1e40af"
      },
      "nodes": [
        { "id": "SYN1", "label": "JSONL 导出" },
        { "id": "SYN2", "label": "JSONL 导入" },
        { "id": "SYN3", "label": "Git 钩子" },
        { "id": "SYN4", "label": "哈希碰撞检测" }
      ]
    },
    {
      "title": "协议层",
      "icon": "🔌",
      "color": {
        "bg": "bg-blue-100",
        "border": "border-blue-600",
        "textColor": "#1e40af"
      },
      "nodes": [
        { "id": "RPC1", "label": "RPC 客户端/服务器" },
        { "id": "RPC2", "label": "Unix 域套接字" },
        { "id": "RPC3", "label": "Windows 命名管道" },
        { "id": "RPC4", "label": "Dolt 服务器模式" }
      ]
    },
    {
      "title": "数据模型",
      "icon": "📋",
      "color": {
        "bg": "bg-blue-100",
        "border": "border-blue-600",
        "textColor": "#1e40af"
      },
      "nodes": [
        { "id": "DM1", "label": "Issue - 核心工作项" },
        { "id": "DM2", "label": "Dependency - 依赖关系" },
        { "id": "DM3", "label": "Label, Comment, Event" },
        { "id": "DM4", "label": "内部类型" }
      ]
    }
  ]
}
```

## 3. 云服务需求详细分析

### 3.1 计算资源需求

**CPU 和内存**:
- 单机开发:≥2 核心,≥4GB RAM
- 小规模团队(1-10 人):≥4 核心,≥8GB RAM
- 中等规模(10-100 人):≥8 核心,≥16GB RAM (Dolt 服务器模式)

**并发需求**:
- 本地 CLI 使用:单进程,支持文件锁防止数据竞争
- Dolt 服务器模式:支持多个 RPC 连接
- 峰值 QPS:单 Dolt 实例 ~100-500 QPS

**用途**:
- CLI 工具执行
- Dolt 数据库操作
- Git 同步
- AI 服务调用

**成本优势**:
- Dolt 嵌入式:0 额外云成本(本地部署)
- 按需扩展

### 3.2 数据库需求

**主数据库:Dolt**
- **类型**:版本控制 SQL (MySQL 兼容语法)
- **配置**:嵌入式或服务器模式
- **存储**:
  - 小规模(1000 期):~50MB
  - 中等规模(10000 期):~500MB
  - 大规模(100000 期):~5GB
- **扩展**:自动版本历史,分支和合并

**用途**:
- 问题追踪存储
- 依赖关系图
- 审计追踪
- 版本历史

**特殊需求**:
- ACID 事务支持
- 全文搜索索引
- JSON 路径查询
- 自动迁移系统

### 3.3 存储需求

**JSONL 文件存储**:
- **容量**:
  - 1000 期:~1-5MB
  - 10000 期:~10-50MB
  - 100000 期:~100-500MB
- **用途**:
  - Git 追踪的问题数据
  - 分布式同步
  - 人类可读格式
  - 增量导出

**访问模式**:S3 兼容 API (如需云存储)
**成本**:主要依赖 Git 托管,无额外对象存储成本

### 3.4 向量数据库需求

**可选**(不是核心):
- 仅当需要语义搜索时
- 向量嵌入标题和描述
- 支持 AI 驱动的相似性检测

**选项**:
- Pinecone (云托管):$70-280/月
- Weaviate (自托管):计算成本
- Qdrant (自托管):计算成本
- 无向量数据库也能工作(全文搜索足够)

### 3.5 AI 服务需求

**嵌入模型**:
- OpenAI Embeddings (text-embedding-3-small)
- 成本:$0.02/1M tokens

**LLM 服务**:
- Anthropic Claude API (优先,用于压缩)
- OpenAI GPT-4 (替代品)
- 本地 LLM (Ollama,可选)

**成本**:
- 小规模:~$10-50/月
- 中等规模:~$100-500/月
- 大规模:~$1000+/月

**外部服务选项**:
- Anthropic Claude API
- OpenAI API
- Hugging Face Inference
- 本地部署开源 LLM

### 3.6 网络和 CDN

**Git 托管(必需)**:
- GitHub、GitLab、Gitea 等
- 用于分布式同步

**全球节点**:
- 不需要 CDN(数据库是本地的)
- Git 托管提供地理冗余

**网络安全**:
- SSH/HTTPS for Git
- TLS for Dolt 服务器模式
- 可选 VPN/专有网络

**SSL/TLS**:自动证书(Git 托管提供)
**带宽**:
- 本地使用:0 带宽
- 团队同步:~1-10MB/天

### 3.7 部署复杂度评估

| 维度 | 评分 (1-10) | 说明 |
|------|-------------|------|
| **基础设施配置** | 2 | CLI 工具,无额外基础设施;可选 Dolt 服务器增加到 4 |
| **数据库管理** | 3 | Dolt 内嵌,自动处理;版本历史内置 |
| **CI/CD 复杂度** | 3 | Go 单二进制;跨平台编译 |
| **监控和日志** | 4 | 本地审计追踪;可选远程日志 |
| **总体复杂度** | 3 | 开发友好,运维开销最小 |

### 3.8 成本估算(月度)

**小规模部署(1000 活跃用户)**
- Dolt 嵌入式:$0
- Git 托管 (GitHub Free):$0
- AI 服务 (可选):$50
- **总计**:**$50/月**

**中等规模(10000 用户)**
- Dolt 服务器:$200-500 (1x4核,8GB RAM)
- Git 托管 (GitHub Pro):$4
- AI 服务:$300-500
- 存储:$50
- **总计**:**$554-1054/月**

### 3.9 必需的云服务清单

✅ **必需**:
- Git 托管 (GitHub/GitLab/Gitea)

⚠️ **推荐**:
- Dolt 服务器 (多用户场景)
- AI/LLM API (AI 驱动功能)

🔧 **可选**:
- 向量数据库 (语义搜索)
- 日志/监控 (可观测性)
- CI/CD 流水线

---

## 4. 核心模块详解

### 4.1 CLI 命令层

**位置**:`cmd/bd/`
**文件数量**:444+ Go 文件
**架构**:Cobra 框架,每个命令一个文件

**主要命令组**:
- 工作流:create, update, close, ready, show
- 依赖管理:dep add, dep remove, dep list
- 查询:list, query, search, ready
- 同步:sync, import, export
- 高级:compact (内存压缩), audit (审计), daemon
- 配置:config, init, setup
- 数据维护:admin, cleanup, merge, rename

### 4.2 存储层

**目录结构**:
```
internal/storage/
├── dolt/              # 主后端:版本控制 SQL
│   ├── dolt.go
│   ├── server.go
│   ├── credentials.go
│   ├── migrations/
│   └── ...
├── sqlite/            # 遗留后端
├── memory/            # 测试后端
└── storage.go         # 通用接口
```

**核心接口**:
```go
type Storage interface {
    CreateIssue(ctx context.Context, issue *Issue) error
    UpdateIssue(ctx context.Context, issue *Issue) error
    GetIssue(ctx context.Context, id string) (*Issue, error)
    ListIssues(ctx context.Context, filter *IssueFilter) ([]*Issue, error)
    AddDependency(ctx context.Context, dep *Dependency) error
    RunInTransaction(ctx context.Context, fn func(Transaction) error) error
}
```

### 4.3 数据模型

**Issue 结构**:
```go
type Issue struct {
    ID               string           // bd-a1b2
    Title            string
    Description      string
    Status           Status           // open, in_progress, blocked, closed, etc.
    Priority         int              // 0-5
    IssueType        IssueType        // bug, feature, task, epic, chore
    Assignee         string
    Dependencies     []*Dependency
    Labels           []string
    Comments         []*Comment
    Metadata         json.RawMessage
    CreatedAt        time.Time
    UpdatedAt        time.Time
    ClosedAt         *time.Time
    CompactionLevel  int
}
```

**依赖类型**:
```go
type DependencyType string
const (
    DepBlocks             DependencyType = "blocks"
    DepRelated           DependencyType = "related"
    DepParentChild       DependencyType = "parent_child"
    DepDiscoveredFrom    DependencyType = "discovered_from"
    DepConditionalBlocks DependencyType = "conditional_blocks"
)
```

### 4.4 同步机制

**写路径**:
```
CLI 命令 → Dolt 数据库 → FlushManager (5秒防抖) → JSONL 文件 → Git 提交
```

**读路径**:
```
Git Pull → 自动检测 → JSONL 导入 → Dolt 数据库更新 → CLI 输出
```

**哈希碰撞防御**:
- 基于 UUID 的哈希 ID (bd-a1b2)
- 内容哈希用于变更检测
- 导入合并:同 ID + 不同内容 = 更新

### 4.5 内存压缩

**实现**:`internal/compact/`
- 语义压缩旧的已关闭期
- TTL 策略
- 使用 Claude Haiku 生成摘要
- 保留原始大小用于审计

## 5. 技术栈

### 5.1 后端

- **语言**:Go 1.25.6
- **CLI 框架**:Cobra
- **配置管理**:Viper
- **数据库**:Dolt (版本控制 SQL)
- **UI 组件**:Lipgloss, Bubbletea, Huh
- **工具库**:UUID, YAML, JSON

### 5.2 前端

- **npm 包**:@beads/bd
- **跨平台**:macOS/Linux/Windows
- **Node**:14+

### 5.3 数据库

- **Dolt**(主要):版本控制 SQL,嵌入式引擎
- **SQLite**(遗留):快速本地查询

### 5.4 AI 服务

- **嵌入**:OpenAI Embeddings
- **LLM**:Anthropic Claude API
- **本地**:Ollama (可选)

### 5.5 认证

- **Git**:SSH 密钥,HTTPS,PAT
- **API**:Bearer 令牌,API 密钥

## 6. 部署架构

### 开发环境

```bash
git clone https://github.com/steveyegge/beads.git
cd beads
go build -o bd ./cmd/bd

cd your-project
../beads init --prefix myapp
../beads create "First feature" -p 0
```

### 生产环境

**单机部署**:
```
应用服务器
├── bd 二进制
├── .beads/dolt/ (本地数据库)
└── .beads/issues.jsonl (Git 追踪)
```

**团队部署**:
```
多个开发机 → Dolt 服务器 → Git 仓库
```

## 7. 工程实践

### 7.1 代码质量

- **Linting**:golangci-lint
- **格式化**:gofmt
- **Type Checking**:Go 编译器原生

### 7.2 测试

```bash
go test ./...              # 快速测试
go test -short ./...       # 单元测试
make test                  # 完整套件
make bench                 # 基准测试
```

### 7.3 CI/CD

**GitHub Actions**:
- 版本一致性检查
- 跨平台测试
- 覆盖率报告
- 自动发布

## 8. 安全和隐私

### 8.1 加密

- **传输中**:Git SSH/HTTPS,TLS
- **静态**:文件系统权限,可选 git-crypt

### 8.2 访问控制

- 本地权限控制
- Git 访问认证
- RPC 隔离 (Unix 套接字/Windows 管道)

### 8.3 审计

- Event 表完整审计追踪
- actor 字段记录操作者
- 纳秒级时间戳

## 9. 性能优化

### 9.1 缓存

- Dolt 查询缓存
- 已阻止期缓存
- 标签/依赖缓存

### 9.2 索引

```sql
CREATE INDEX idx_issues_status ON issues(status);
CREATE INDEX idx_dependencies_from ON dependencies(from_id);
CREATE FULLTEXT INDEX idx_issues_title ON issues(title);
```

### 9.3 流式处理

- 流式 JSONL 写入
- 分批处理 (batch_size=1000)
- 适用于 100K+ 期

### 9.4 异步队列

- 防抖刷新 (默认 5秒)
- FlushManager 后台处理
- 可配置延迟

## 10. 总结

Beads 的架构设计体现了以下核心原则:

1. **分布式优先** - 无中央服务器,通过 Git 分布
2. **版本控制集成** - Dolt 原生版本历史
3. **冲突避免** - 哈希 ID 防止并发碰撞
4. **AI 优化** - JSON 输出,依赖图,自动检测
5. **极简部署** - 单二进制,最小运维开销

**适用场景**:
- AI 编码智能体任务追踪
- 分布式团队协作
- 长期项目规划和记忆
- 开发者自主管理工作流

**云服务推荐**:Git 托管 + Dolt (嵌入式/服务器) + 可选 AI API

---

**文档版本**:v1.0
**更新日期**:2025-02-11
**基础版本**:Beads v0.49.6
